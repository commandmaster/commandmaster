const { MonoBehaviour } = ScriptingAPI;

export default class <%= className %> extends MonoBehaviour {
    constructor(engineAPI, gameObject) {
        super(engineAPI, gameObject);
    }

    Start() {
        
    }

    Update() {
        
    }
}

